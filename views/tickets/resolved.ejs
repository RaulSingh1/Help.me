<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Resolved Tickets - Help Desk</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('../partials/nav') %>

    <div class="container">
        <h1>Resolved Tickets</h1>
        
        <div class="actions">
            <a href="/tickets" class="btn">View Open Tickets</a>
            <a href="/tickets/search" class="btn">Search Tickets</a>
        </div>

        <% if (tickets.length === 0) { %>
            <p>No resolved tickets.</p>
        <% } else { %>
            <table>
                <thead>
                    <tr>
                        <th>Title</th>
                        <th>Priority</th>
                        <th>Created By</th>
                        <th>Resolved Date</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% tickets.forEach(ticket => { %>
                        <tr>
                            <td><%= ticket.title %></td>
                            <td><%= ticket.priority %></td>
                            <td><%= ticket.user ? ticket.user.username : 'Unknown' %></td>
                            <td><%= ticket.resolvedAt ? ticket.resolvedAt.toLocaleDateString() : 'Unknown' %></td>
                            <td>
                                <a href="/tickets/<%= ticket._id %>">View</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } %>
    </div>
</body>
</html>

