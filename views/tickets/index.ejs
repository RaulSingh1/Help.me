<!DOCTYPE html>
<html lang="no">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alle saker - Hjelpesenter</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <%- include('../partials/nav') %>

    <div class="container">
        <% const canSeeDeletedContent = user && user.isAdmin && showDeleted; %>
        <% const statusLabels = { open: 'Åpen', 'in-progress': 'Under arbeid', resolved: 'Løst', closed: 'Lukket' }; %>
        <% const priorityLabels = { low: 'Lav', medium: 'Middels', high: 'Høy', urgent: 'Kritisk' }; %>

        <h1>Alle saker</h1>

        <div class="actions">
            <a href="/tickets/new" class="btn">Opprett ny sak</a>
            <a href="/tickets/search<%= user && user.isAdmin && showDeleted ? '?showDeleted=1' : '' %>" class="btn">Søk i saker</a>
            <a href="/tickets/resolved<%= user && user.isAdmin && showDeleted ? '?showDeleted=1' : '' %>" class="btn">Se løste saker</a>
            <% if (user && user.isAdmin) { %>
                <a href="/tickets?showDeleted=<%= showDeleted ? '0' : '1' %>" class="btn btn-secondary"><%= showDeleted ? 'Skjul slettet' : 'Vis slettet' %></a>
            <% } %>
        </div>

        <% if (tickets.length === 0) { %>
            <p>Ingen saker er opprettet ennå.</p>
        <% } else { %>
            <table>
                <thead>
                    <tr>
                        <th>Tittel</th>
                        <th>Prioritet</th>
                        <th>Status</th>
                        <th>Opprettet av</th>
                        <th>Dato</th>
                        <th>Handlinger</th>
                    </tr>
                </thead>
                <tbody>
                    <% tickets.forEach(ticket => { %>
                        <% const isDeletedTicket = Boolean(ticket.deletedAt); %>
                        <% const hideDeletedContent = isDeletedTicket && !canSeeDeletedContent; %>
                        <% const preview = hideDeletedContent || !ticket.description ? '' : (ticket.description.length > 180 ? ticket.description.slice(0, 180) + '…' : ticket.description); %>
                        <tr class="<%= isDeletedTicket ? 'row-deleted' : '' %>">
                            <td data-label="Tittel" class="cell-primary">
                                <span class="ticket-title"><%= hideDeletedContent ? '[slettet sak]' : ticket.title %></span>
                                <% if (preview) { %>
                                    <p class="ticket-preview"><%= preview %></p>
                                <% } %>
                            </td>
                            <td data-label="Prioritet" class="cell-meta priority-chip <%= hideDeletedContent ? '' : ('priority-' + ticket.priority) %>"><%= hideDeletedContent ? '[slettet]' : (priorityLabels[ticket.priority] || ticket.priority) %></td>
                            <td data-label="Status" class="cell-meta"><%= isDeletedTicket ? 'Slettet' : (statusLabels[ticket.status] || ticket.status) %></td>
                            <td data-label="Opprettet av" class="cell-meta"><%= hideDeletedContent ? '[slettet]' : (ticket.user ? ticket.user.username : 'Ukjent') %></td>
                            <td data-label="Dato" class="cell-meta"><%= hideDeletedContent ? '[slettet]' : (ticket.dateCreated ? ticket.dateCreated.toLocaleDateString('no-NO') : 'Ukjent') %></td>
                            <td data-label="Handlinger" class="cell-action">
                                <a href="/tickets/<%= ticket._id %><%= user && user.isAdmin && showDeleted ? '?showDeleted=1' : '' %>">Vis</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        <% } %>
    </div>
</body>
</html>
